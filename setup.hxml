--cmd haxelib install ase 2.1.1 --always

--next
--cmd haxelib install uuid 2.4.1 --always

--next
--cmd haxelib install format 3.8.0 --always

--next
--cmd haxelib dev castle git/castle --always

--next
--cmd haxelib dev heaps git/heaps --always

--next
--cmd haxelib dev hxnodejs git/hxnodejs --always

--next
--cmd haxelib dev electron git/hxelectron --always

--next
--cmd haxelib dev heaps-aseprite git/heaps-aseprite --always

--next
--cmd haxelib dev deepnightLibs git/deepnightLibs --always

--next
--cmd haxelib dev ldtk-haxe-api git/ldtk-haxe-api --always
